# 国内问卷平台文本导入实测

测试日期：2026-08-23

测试对象：

- 问卷星“从文本创建问卷”编辑器
- 腾讯问卷“文本编辑器”

所有结论均来自登录后的真实编辑页和右侧实时预览，不是根据帮助文档推测。测试期间没有点击问卷星“完成”、腾讯问卷“发布/分享”，结束后两个编辑器均恢复为原始示例。

## 先说最容易踩的坑

| 输入情况 | 问卷星 | 腾讯问卷 |
| --- | --- | --- |
| 题目之间一个或两个真正的空行 | 正常 | 正常 |
| 选择题题目与第一个选项之间有空行 | 有 `A./B.` 前缀时正常；无前缀时可能被拆成填空题 | 报“题目格式不正确” |
| 两个选项之间有空行 | 有 `A./B.` 前缀时正常；无前缀时可能被拆题 | 报“题目格式不正确” |
| 空白行实际只含零宽空格 `U+200B` | 生成一个没有文字的单选项 | 不应输入，未作为合法格式使用 |
| 空白行含空格、全角空格、NBSP 或 Tab | 无前缀选项可能与题目粘连并被拆成多道填空题 | 不应输入；选择题内部空行本身就非法 |
| 未声明题型的“欢迎语/注释” | 被当成一道必填填空题 | 第一题前的第二段非空文本可作为欢迎语 |
| 题型标记后残留空格 | 题目行可能失效，后续内容被错误拼成填空题 | 直接报格式错误，右侧保留上一次有效预览 |

问卷星里真正复现“空选项”的不是普通空行，而是一整行肉眼不可见的 `U+200B`。普通空行也并非绝对安全：当选择题选项没有 `A./B.` 等前缀时，空行会改变题目分组，造成题目与选项粘连、选项变成新填空题。

## 问卷星

### 已实际生成的题型

- 无题型标记、有多行选项：单选题
- `[多选题]`
- 无选项的编号题目：单行填空题
- 题目中使用 `________`：一行内多个填空框
- `[量表题]`
- `[矩阵题]`
- `[排序题]`
- `[段落说明]`
- `[比重题]`
- `[矩阵量表题]`
- `[表格题]`

`[表格题]` 虽能生成，但表头、下拉选项和行名的映射不直观。本轮只能确认题型标记有效，不能把当前样例当作稳定模板。

### 解析特点

- 第一条非空文本会成为问卷标题。
- 标题后的普通未编号文本不会成为欢迎语，而会成为必填填空题。说明文字应显式使用 `[段落说明]`。
- `A.选项`、`B. 选项`、`C、选项`、`D 选项`、`E选项` 都会各自成为一个完整选项；这些前缀会原样保留，不会被剥掉。
- 选择题使用 `A./B.` 前缀后，普通空行的容错明显更好。
- 单选按官方示例使用题目行加 `A.选项`、`B.选项`；多选按官方示例使用 `题目[多选题]`，选项逐行直接写，不加字母前缀。
- 段落说明按官方示例使用 `文字[段落说明]`，不在标签前插入空格。
- `[量表题]` 按官方示例使用 `题目[量表题]`，随后每个刻度单独占一行，例如 `1`、`2`、`3`、`4`、`5`。它不是腾讯问卷的 `1(低)~5(高)` 范围写法；当前文本格式也不能传递两端说明文字。

### 推荐的最小模板

```text
问卷标题

1.第一部作品
A.推荐
B.不推荐

2.第二部作品[多选题]
作画
演出
音乐

3.补充说明[段落说明]
本段说明文字
```

这不是最短写法，而是本轮实测后更不容易被空行和 AI 排版破坏的写法。

## 腾讯问卷

### 已实际生成的题型

- `[单选题]`
- `[多选题]`
- `[单行文本题]`
- `[多行文本题]`
- `[量表题]`
- `[下拉题]`
- `[排序题]`

`[矩阵题]` 在本轮实测中直接报“题目格式不正确”，没有生成矩阵。

### 解析特点

- 第一条非空文本是问卷标题。
- 第一题之前的第二段非空文本会成为欢迎语，不会生成题目。
- 一道选择题内部不能出现空行：题目后空一行或两个选项间空一行都会报错。
- 一道题结束后再空一行或两行，然后写下一条带题型标记的题目，均能正常分题。
- `题目[单选题]` 与 `题目 [单选题]` 均有效。
- `[单选题]` 后面只要残留一个空格，就会报格式错误。
- 题目前的 `1.` 会成为题目正文的一部分；右侧还会另外生成 `01` 的平台题号。
- `A./B.` 等选项前缀会原样显示。若不希望问卷中出现字母，腾讯问卷应直接使用无前缀、连续且不含空行的选项。
- 输入无效时，右侧不会清空，而会继续显示上一次有效预览。自动校验不能只看右侧“还有没有内容”，必须同时检查左侧错误提示。
- `[量表题]` 的实测格式为 `1(低)~5(高)`。

### 推荐的最小模板

```text
问卷标题

欢迎语

第一部作品[单选题]
推荐
不推荐

第二部作品[多选题]
作画
演出
音乐

补充说明[多行文本题]
```

## 生成文本前必须做的清洗

无论文本由人、脚本还是 AI 生成，导入前至少应执行以下处理：

1. 把换行统一为 `LF`。
2. 删除 `U+200B`、`U+200C`、`U+200D`、`U+FEFF` 等零宽字符。
3. 删除每行末尾的空格、全角空格、NBSP 和 Tab；题型标记后不得留下任何字符。
4. 把“只含空白字符的行”归一成真正的空行。
5. 问卷星的选择题统一使用连续的 `A./B./C.` 选项，不在题目内部插入空行。
6. 腾讯问卷的选择题选项连续排列，不在题目与首个选项、两个选项之间插入空行。
7. 生成后检查左侧错误提示和右侧题目数，不能只看右侧是否仍有预览。

## 后续自动化应采用的验证标准

文本生成器不应只输出字符串，还应在导入前给出以下检查结果：

- 是否存在零宽字符或只含不可见字符的行；
- 是否存在题型标记后的尾随空白；
- 问卷星的未编号普通文本是否误充当说明；
- 腾讯问卷的选择题内部是否有空行；
- 预期题目数、单选项数、多选项数是否与解析预览一致。
